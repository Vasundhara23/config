server:
  port: 8083
spring:
  application:
    name: Task-Service
  datasource:
    url: jdbc:mysql://localhost:3306/tasks
    username: root
    password: Mysql@123

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

